import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.answerarchitect',
  appName: 'The Answer Architect',
  webDir: 'out'
};

export default config;
